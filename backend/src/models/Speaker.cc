/**
 *
 *  Speaker.cc
 *  DO NOT EDIT. This file is generated by drogon_ctl
 *
 */

#include "Speaker.h"
#include <drogon/utils/Utilities.h>
#include <string>

using namespace drogon;
using namespace drogon::orm;
using namespace drogon_model::icnari_conference_db;

const std::string Speaker::primaryKeyName = "";
const bool Speaker::hasPrimaryKey = false;
const std::string Speaker::tableName = "speaker";

const std::vector<typename Speaker::MetaData> Speaker::metaData_={
};
const std::string &Speaker::getColumnName(size_t index) noexcept(false)
{
    assert(index < metaData_.size());
    return metaData_[index].colName_;
}
Speaker::Speaker(const Row &r, const ssize_t indexOffset) noexcept
{
    if(indexOffset < 0)
    {
    }
    else
    {
        size_t offset = (size_t)indexOffset;
        if(offset + 0 > r.size())
        {
            LOG_FATAL << "Invalid SQL result for this model";
            return;
        }
        size_t index;
    }

}

Speaker::Speaker(const Json::Value &pJson, const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 0)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
}

Speaker::Speaker(const Json::Value &pJson) noexcept(false)
{
}

void Speaker::updateByMasqueradedJson(const Json::Value &pJson,
                                            const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 0)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
}

void Speaker::updateByJson(const Json::Value &pJson) noexcept(false)
{
}

void Speaker::updateId(const uint64_t id)
{
}

const std::vector<std::string> &Speaker::insertColumns() noexcept
{
    static const std::vector<std::string> inCols={
    };
    return inCols;
}

void Speaker::outputArgs(drogon::orm::internal::SqlBinder &binder) const
{
}

const std::vector<std::string> Speaker::updateColumns() const
{
    std::vector<std::string> ret;
    return ret;
}

void Speaker::updateArgs(drogon::orm::internal::SqlBinder &binder) const
{
}
Json::Value Speaker::toJson() const
{
    Json::Value ret;
    return ret;
}

Json::Value Speaker::toMasqueradedJson(
    const std::vector<std::string> &pMasqueradingVector) const
{
    Json::Value ret;
    if(pMasqueradingVector.size() == 0)
    {
        return ret;
    }
    LOG_ERROR << "Masquerade failed";
    return ret;
}

bool Speaker::validateJsonForCreation(const Json::Value &pJson, std::string &err)
{
    return true;
}
bool Speaker::validateMasqueradedJsonForCreation(const Json::Value &pJson,
                                                 const std::vector<std::string> &pMasqueradingVector,
                                                 std::string &err)
{
    if(pMasqueradingVector.size() != 0)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool Speaker::validateJsonForUpdate(const Json::Value &pJson, std::string &err)
{
    return true;
}
bool Speaker::validateMasqueradedJsonForUpdate(const Json::Value &pJson,
                                               const std::vector<std::string> &pMasqueradingVector,
                                               std::string &err)
{
    if(pMasqueradingVector.size() != 0)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool Speaker::validJsonOfField(size_t index,
                               const std::string &fieldName,
                               const Json::Value &pJson,
                               std::string &err,
                               bool isForCreation)
{
    switch(index)
    {
        default:
            err="Internal error in the server";
            return false;
    }
    return true;
}
