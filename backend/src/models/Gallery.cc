/**
 *
 *  Gallery.cc
 *  DO NOT EDIT. This file is generated by drogon_ctl
 *
 */

#include "Gallery.h"
#include <drogon/utils/Utilities.h>
#include <string>

using namespace drogon;
using namespace drogon::orm;
using namespace drogon_model::icnari_conference_db;

const std::string Gallery::primaryKeyName = "";
const bool Gallery::hasPrimaryKey = false;
const std::string Gallery::tableName = "gallery";

const std::vector<typename Gallery::MetaData> Gallery::metaData_={
};
const std::string &Gallery::getColumnName(size_t index) noexcept(false)
{
    assert(index < metaData_.size());
    return metaData_[index].colName_;
}
Gallery::Gallery(const Row &r, const ssize_t indexOffset) noexcept
{
    if(indexOffset < 0)
    {
    }
    else
    {
        size_t offset = (size_t)indexOffset;
        if(offset + 0 > r.size())
        {
            LOG_FATAL << "Invalid SQL result for this model";
            return;
        }
        size_t index;
    }

}

Gallery::Gallery(const Json::Value &pJson, const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 0)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
}

Gallery::Gallery(const Json::Value &pJson) noexcept(false)
{
}

void Gallery::updateByMasqueradedJson(const Json::Value &pJson,
                                            const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 0)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
}

void Gallery::updateByJson(const Json::Value &pJson) noexcept(false)
{
}

void Gallery::updateId(const uint64_t id)
{
}

const std::vector<std::string> &Gallery::insertColumns() noexcept
{
    static const std::vector<std::string> inCols={
    };
    return inCols;
}

void Gallery::outputArgs(drogon::orm::internal::SqlBinder &binder) const
{
}

const std::vector<std::string> Gallery::updateColumns() const
{
    std::vector<std::string> ret;
    return ret;
}

void Gallery::updateArgs(drogon::orm::internal::SqlBinder &binder) const
{
}
Json::Value Gallery::toJson() const
{
    Json::Value ret;
    return ret;
}

Json::Value Gallery::toMasqueradedJson(
    const std::vector<std::string> &pMasqueradingVector) const
{
    Json::Value ret;
    if(pMasqueradingVector.size() == 0)
    {
        return ret;
    }
    LOG_ERROR << "Masquerade failed";
    return ret;
}

bool Gallery::validateJsonForCreation(const Json::Value &pJson, std::string &err)
{
    return true;
}
bool Gallery::validateMasqueradedJsonForCreation(const Json::Value &pJson,
                                                 const std::vector<std::string> &pMasqueradingVector,
                                                 std::string &err)
{
    if(pMasqueradingVector.size() != 0)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool Gallery::validateJsonForUpdate(const Json::Value &pJson, std::string &err)
{
    return true;
}
bool Gallery::validateMasqueradedJsonForUpdate(const Json::Value &pJson,
                                               const std::vector<std::string> &pMasqueradingVector,
                                               std::string &err)
{
    if(pMasqueradingVector.size() != 0)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool Gallery::validJsonOfField(size_t index,
                               const std::string &fieldName,
                               const Json::Value &pJson,
                               std::string &err,
                               bool isForCreation)
{
    switch(index)
    {
        default:
            err="Internal error in the server";
            return false;
    }
    return true;
}
